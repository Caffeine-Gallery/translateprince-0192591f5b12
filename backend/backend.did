service : {
  addTranslation: (text, text, text) -> ();
  getTranslations: () -> (vec record {
                                text;
                                text;
                                text;
                              }) query;
}
